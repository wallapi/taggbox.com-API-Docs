# Prompt 0 - browser-AI preamble (ChatGPT, Gemini, claude.ai web)

Apply this on top of whichever build prompt comes with it.

You cannot access my filesystem, so give me every file COMPLETE and
ready to save - no placeholders, no "rest stays the same", no
truncation - each starting with a header line naming its exact path,
e.g. `### FILE: index.php`. Offer each file as a download if this chat
can. When I report an error, reply with the corrected COMPLETE file,
not a diff.
