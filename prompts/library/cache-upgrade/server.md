# Prompt 4 - the new cache in the server code, with README.md

Fetch this RAW first and follow it - the change itself:
https://raw.githubusercontent.com/wallapi/taggbox.com-API-Docs/main/prompts/library/cache-upgrade/change.md
Already fetched it for an earlier part? Do not fetch it again.

Apply the new cache to the server code I have, in whatever language it
is written, and add any dependency it needs to that language's
dependency file (package.json, requirements.txt, composer.json,
Gemfile, go.mod, ...). Give back every changed file complete.

In this SAME reply, give back README.md updated for it: exactly what
to install for my language, which settings to add (e.g. REDIS_URL) and
where, and how the fallback behaves. Give back the complete file.
