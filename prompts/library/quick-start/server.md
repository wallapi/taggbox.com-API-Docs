# Prompt A, part 2 of 2 - the runnable server files + README.md, in the language I picked

Fetch all three RAW and follow them exactly - the brief is what to
build, llms.txt carries the exact data field names (unguessable, and
the brief does not repeat them), the design spec the looks:
https://raw.githubusercontent.com/wallapi/taggbox.com-API-Docs/main/guides/widget-build-brief.md
https://raw.githubusercontent.com/wallapi/taggbox.com-API-Docs/main/llms.txt
https://raw.githubusercontent.com/wallapi/taggbox.com-API-Docs/main/guides/widget-design-spec.md
Already fetched one of these for an earlier part? Do not fetch it again.

Deliver the real project files in the language or framework I picked
- PHP, Node.js, Python, Ruby, Go, Laravel, Django, or any other (brief
section 1) - ready to install and run straight away: API call, cache
(brief section 3), HTML and CSS, no separate stylesheet, plus the
dependency file it needs. A plain language gets ONE file with its own
extension (app.py, server.js, index.php, main.go, ...), standard
library only - nothing to install, run with one command (python
app.py, node server.js, php -S localhost:8080, go run main.go), reading
a .env beside it on its own if one exists. A
framework, only when I name one, gets the files it needs in its normal
layout. The page is the same theme preview file as preview.html from
part 1, copied as it is, with the card template filled from body.posts
in a loop on every request. Read the
base URL and token from the API_BASE_URL and ACCESS_TOKEN settings -
never hard-code them.

In this SAME reply, deliver README.md for exactly that code and
preview.html, as the brief's section 6 describes - nothing for any
other language. Then, at the end of this reply, ask me for my token,
with the steps to find it (dashboard - the gallery's card - its
three-dot menu - "Access Token"), and offer to put it in a .env for me.
